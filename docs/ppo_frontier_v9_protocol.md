# 优化 PPO 的完整实验重跑协议（v9）

用户于 2026-09-09 明确要求：按优化后的 PPO 逻辑重新训练大小规模两份 PT，按原《数值实验与结果分析_论文稿.md》规格重跑全部实验，并由额外智能体独立核验交付完整性。最初要求重新求解 NSGA-II，随后明确预算不变并允许复用；最终执行后者。只更新 Markdown 文稿，不生成或更新 Word；本次未要求 GitHub 提交或推送。

## 参考论文预算核验

本地 `参考文献/Chen 等 - 2026 - Integrated hybrid energy and time-of-use electricity tariffs for the resource-constrained project sc.pdf`，第 15 页第 5.3 节及第 16 页表 7/8 明确使用 1000/3000 **iterations**。已进行文本提取和整页视觉核验。文中没有将其定义为候选尝试次数，也没有足够的 NSGA-II 种群/评价计数细节用于严格换算。

本仓库 `src/pareto_experiment.py::nsga_fronts` 则按候选尝试计数，种群为 100，每个完整子代批生成 100 个候选，初始种群新增 99 个候选也计入预算。故不能将原文的 1000/3000 直接宣称为本仓库 1000/3000 次尝试。用户随后明确保持原预算并允许复用 NSGA-II：本轮 B1=40320、B2=120960 次候选尝试，不改为秒数，不采用论文迭代数。复用 v4 的12次演化及15份预算档案；原文件字节、原协议哈希、原种子、方案与耗时不变，新协议单独记录来源哈希，而不是把旧实验改名为新运行。

## 不变的数据及记录

- 逐字节复用 v4 的五个基础案例：small-fixed、Test-1 至 Test-4；small-fixed 与 Test-1 不混用。
- 敏感性复用 v4 的八个非基准参数版本和同一 Test-4 基准，不再生成实例或重新绑定 b。
- 两份新模型各用原 v4 对应规模的 24 个训练实例；训练/测试数据哈希无交集。所有输入、奖励及择优读取当前实例预绑定 b。
- 旧 PT、NSGA 前沿、原实验目录保留；旧 Markdown 正文在更新前备份。本轮输出独立于 v4/v7/v8。

## 新训练与求解口径

- 网络复用 v8 优选：96 维、1 层 Transformer、三个独立参数且读取前序选择的 512 类对象头、原线性标量价值输出；直接使用处理能力严格掩码。
- 每个规模 48 次 rollout 更新 × 32 步 × 24 实例 = 36864 次训练动作，3 epochs、minibatch 96、1152 次 Adam 更新。其余参数沿用 v8：lr=0.0003，entropy=0.02，gamma=0.95，GAE lambda=0.90，clip=0.2，梯度裁剪=0.5；训练 seed=20260909。
- 保留按实例分配的 512/1024/1536 步 episode horizon。按预先固定的 21 偏好网格在 episode 间均衡分配；episode 内偏好不变，done 后重新设置偏好并重算初始状态。整体覆盖全部偏好，不声称每实例都见齐 21 偏好。保存每实例×偏好真实动作计数。
- 只保存预定最终更新检查点，不按测试结果选择中途权重。新 PT 在 `outputs/ppo_frontier_v9/models/`，新登记不覆盖历史登记。
- 按原文稿，每个偏好求解上限为 1920 次候选尝试，21 偏好完整前沿上限为 40320；不是把 v8 的单点 12000 次扩展成每权重 12000。采用优化后批宽 8，满预算相当于 240 批/偏好，温度 2，输入进度 horizon=1024。
- 同状态动作去重只排除已经尝试的完整动作，输入变化即重置；仍从 PPO 剩余联合概率抽取，禁止启发式对象选择/额外评价/跨偏好热启动。根部正概率动作集合耗尽时记录真实提前停止，保留已抽半批并评价，不补虚构计数；非根数值异常单独记录为失败，不能冒充完全穷尽。
- 外部档案收集全部实际生成且严格可行的候选，不仅收集加权目标接受点或 21 个末解。档案不反馈给搜索。缓存、失败、评价次数及候选尝试分开计数。

## 实验交付

- 表4：同一 small-fixed，5 偏好×3 次新 PPO；5 次新 MILP，各 3600 s 上限。PPO 表格取三次最小 J 的同一方案，时间为三次累计；限时 MILP 不冒充最优。
- 表5/6：48 次新 PPO 完整前沿（24 泛化与 24 非基准敏感性）；复用12 次旧 NSGA-II 演化，Test-4 各重复原先继续到 B2，总 15 份 NSGA 预算档案。表5与表6 Train-L/Test-4严格复用，同规模两模型共用 NSGA 基线。旧 NSGA 批次16并发；新 PPO 初始主队列3并发，后将最后12个尚未运行的共载情景任务以独立单worker入口并行调度，重叠上界15，每个求解仍为1线程。入口workers=1/3不是全局并发数，15也不冒充实测全局峰值。时间只是不同争用条件下的墙钟观测，不作严格速度排名。
- 敏感性：9 情景×3 记录，基准复用；同情景三次联合非支配档案，不平均坐标。保留散点、折线、PCHIP 三版及 SVG/300dpi 以上 PNG、源数据及插值追溯。
- 交付原表格结构、方法左列、时间及合法性/端点/风险分解附录。公式保留文字 LaTeX，不生成公式图像。
- 独立智能体负责完整性核验，主代理负责数值及全约束重放。只有全部新产物对应文件哈希通过才能报告完成。

## 当前执行与重建入口

本轮模型登记及全部求解已经完成，不能重新训练或覆盖检查点来继续结果整理。原预算复用协议已冻结，SHA256为 `5452d423c3293be00e5e5ce578b57e2e4cb0bb2c248b5d3ba32e5b555bf59ce5`。下列为历史执行及结果重建入口，不是要求重新启动已经完成的训练或求解：

```powershell
py -B run_ppo_frontier_v9.py prepare --nsga-budgets 40320 120960 --reuse-nsga
py -B run_ppo_frontier_v9.py preflight
py -B -u run_ppo_frontier_v9.py run --kind PPO --workers 3
py -B -u run_ppo_frontier_v9.py small
py -B run_ppo_frontier_v9.py normalize
py -B report_ppo_frontier_v9.py
py -B audit_ppo_frontier_v9.py
```

先检查 `completed/`、`progress/`、`actions/` 及进程状态，避免重复计算。原档案缺失时用 `reuse-nsga` 补充字节复制，不能改写来源哈希。暂存文稿、图表与独立完整性核验齐备后，才运行报告的 `--publish`，再运行 `audit_ppo_frontier_v9.py --require-report`；预检和暂存报告不等于正式文稿终验。

并行辅助任务只调用同一冻结入口与同一精确task ID，预算、模型、种子及算法源码不变。其PID、命令、UTC起止、退出状态和逐任务日志在 `output/pareto-ppo-v9/orchestration/coload-aux-20260909T142107Z/manifest.json`；12项均已完成，主队列随后全部直接复用完成记录，没有重复候选搜索。只有11份辅助入口原始execution记录，`PPO-coload-130-large-r2`的一份缺失，原因未确定；没有补造。该任务仍有命令/PID/起止/日志/完成文件和21偏好动作日志链，缺口由 `execution_links.json` 和正文明确披露，不声称所有执行元数据齐备。

五组MILP已全部结束并完成规范化。原始文件及规范化文件保留原 `proven_optimal` 标志；p3原标志因近整数变量的目标交接问题为false，虽然日志为status0/success/gap0的最优终止。报告层另存solver证明、原/规范化标志、原线性/共同目标差、完整矩阵残差和来源哈希，只有全部检查通过才交接最优状态。这里的最优终止均在声明数值容差内；规范化目标略低于求解器对偶界的微小差异不是新发现的更优解。

## 解释边界

v8 算法结构曾基于固定 Test-4 反馈调整。本轮重新训练不抹去这种测试知情设计，因此不能继续写该案例完全未见或算法未经测试调参。四规模各一实例及每模型一个训练种子仅支持案例描述；三次重复不是跨实例泛化统计。候选预算相同不等于墙钟时间或评价开销相同，需如实记录设备、并行争用、训练成本及实际停止原因。

## 已完成结果与正式数值终验

两份新PT、48次新PPO完整前沿、15次小规模PPO及5次MILP全部完成；15份NSGA预算档案按原字节复用。正式Markdown已发布，SHA256为 `60a436e7d0264149fa29e80f13392078022f221113982c3f3125264360f36861`，对应报告重建记录SHA256为 `62c0f4fccfd14182aa8a5ab1ff619b5992681a2eb9251250e808173496e1cdec`。

正式数值终验命令 `py -B audit_ppo_frontier_v9.py --require-report` 已PASS，结果 `output/pareto-ppo-v9/audit/numerics-with-report.json` SHA256为 `4f5aa75f63635b1adb27f502c290b5547839839fb7e4a3d133add74b0be8b806`。检查63份前沿档案、1023组真实动作日志、27组独立广播重算覆盖率、5组同一实际方案的表4择优/成本/风险/目标/累计时间，以及5组MILP原始—规范化证明交接。13案例3768份按实例去重保存方案通过原MILP矩阵、变量界及整数性检查，0违规，最大行残差1.4211e-14；MILP求解器返回的5组完整变量向量另外检查，其规范化残差上限为7.11e-6，不能混淆这两组残差口径。

162项单元测试通过，三版敏感性图逐图视觉记录为 `audit/visual-qa-v9.md`。独立完整性核验另行确认表4—6、附表、三版图、原始数据与预算追溯，保留上文的一份执行元数据缺口。动作日志审核不等于独立重执行每个候选的全部神经概率。初次 `audit/numerics.json` 已标明不是正式交付审核：其运行中修正了审计CSV路径，因此当次未执行独立覆盖CSV检查；后续正式终验重新运行全部检查并要求审核源码始末哈希一致。

主要结论：小规模PPO有2/5组达到MILP最优目标；Train-L在Test-3相对本次NSGA基准为N→P=0、P→N=1，但Test-4仍落后，B1为N→P=0.7781±0.1307、P→N=0，B2为N→P=1、P→N=0。此前v8单点12000候选胜出不能套用于本轮多偏好模型、每偏好1920候选的完整前沿比较。不追加预算以改变结论。旧文稿、旧模型及原NSGA档案保留，未更新Word或提交推送GitHub。
