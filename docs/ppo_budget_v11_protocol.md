# 冻结 V9：全部 PPO 的 12000 候选重跑

`v11` 仅是本次结果与入口的隔离归档编号，不代表换成第11版PPO；实际算法、网络和两份PT均为用户指定的V9。

## 范围与不变项

用户最终指定：使用 V9 两份现有 PT，不重新训练，全部 PPO 实验每偏好上限 12000 个候选。参数、实例、各实例预先绑定的 b、偏好网格和种子沿用 V9。两个模型各自训练来源仍为24个TRAIN实例、36864动作、1152 Adam更新；这些都是历史训练记录，本轮没有训练。

- 小模型：`outputs/ppo_frontier_v9/models/small_model.pt`，SHA256 `2d22202e39eeb681b38f1dd34dcf84f55c125828877cd66c1241ec2a7fdfcf00`。
- 大模型：`outputs/ppo_frontier_v9/models/large_model.pt`，SHA256 `0269a9a52a6e25ef1ed9d89ba8861ba35aa09e5d9b37643410baea1ee64fce54`。
- 登记：`configs/ppo_models_frontier_v9.json`。不替换为 V8 PT，不采用 V10 探索参数。
- 仍为96维、1层Transformer、三个条件化独立512对象头、PPO选择全部对象、处理能力掩码及原价值头。
- 冻结推理仍为CPU、每进程1线程；宽度8、温度2、horizon1024、同输入完整动作去重。调度配置16 workers，不等于每个时刻实测并行峰值。

## 数量与预算

| 实验 | 新求解次数 | 每次预算上限 |
| --- | --- | --- |
| 小固定案例，5偏好×3重复 | 15 | 12000 |
| 跨规模，两模型×四规模×3重复 | 24完整前沿 | 21×12000=252000 |
| 八个非基准敏感性情景×3重复 | 24完整前沿 | 252000 |
| 大规模前沿比较及敏感性基准 | 复用新Train-L/Test-4三前沿 | 不额外计算 |

每个所需规模仍只用一个固定实例。小固定案例另有独立实例，不与Test-1混同。三次完整前沿逐次配对NSGA-II计算覆盖，再汇总均值和样本标准差；小固定案例按原口径三次选最小J实际方案、时间三次求和。敏感性图采用三次前沿的联合非支配档案，不平均坐标。

候选是求解阶段完整动作尝试，不是训练更新、成功移动或目标评价次数。不可行、无变化与拒绝动作也计数。正概率动作空间耗尽时记录真实提前停止和余量；不补假次数、不转移到其他偏好。完整动作日志损失为零，以gzip压缩JSONL保存，终验完整读到EOF检查CRC。

## 历史对照与公平性边界

NSGA-II复用v4的12次演化、15份B1/B2档案及1046个唯一保存方案，B1=40320、B2=120960。MILP复用V9五组3600秒上限求解的18份原文件，源文件与副本逐字节核验，不新增求解、不修改原标志。报告层单独重新推导容差内最优性交接状态。

本轮PPO完整前沿上限是B1的6.25倍、B2的约2.0833倍，不是等预算实验。历史NSGA-II与新PPO计时不在同一批资源争用条件下，不能据时间直接声称公平加速倍数。实例经既往探索使用，不能将本轮描述为无偏多实例泛化验证。

## 来源、回退与运行

新目录为 `output/pareto-ppo-budget-v11/`。协议SHA256为 `db90a62fa5dab3d2180e1f640239fba195fe6d9470382f6c91ecdcee96e68ce5`。回退包在 `backups/ppo-v9-before-budget-v11/`，其中包含原正式Markdown和源代码/模型压缩包及SHA256清单。

已完成准备和1920候选前缀预检：同一冻结大模型及种子，解压后动作日志与V9原日志逐字节相同。预检不是正式新增重复。

```powershell
py -B run_ppo_budget_v11.py prepare
py -B run_ppo_budget_v11.py preflight
py -B run_ppo_budget_v11.py small
py -B -u run_ppo_budget_v11.py run --workers 16
py -B report_ppo_budget_v11.py
py -B audit_ppo_budget_v11.py
py -B report_ppo_budget_v11.py --publish
py -B audit_ppo_budget_v11.py --require-report
```

正式运行已经完成：48次完整前沿及15次小规模求解，失败0，不重复启动。每任务有独占claim、真实actual_execution及完成文件哈希；失败记录保留，不伪造起止时间或静默覆盖日志。small已有文件恢复分支不自行重新验证，故终验逐个核对15份来源/模型/偏好/种子/日志/预算，不能以文件存在视为成功。

报告默认暂存，完成完整约束、数值、图表及独立完整性核验后才发布正式Markdown，不更新Word。整体PASS须与最终实际交付文件哈希一致。完成后按用户要求提交并推送main，保留历史数据与失败探索，不强推。

## 本轮完成记录

前沿实际尝试9589192/12096000，259个偏好提前耗尽；小规模实际17040/180000，15次均提前耗尽。两份V9 PT及训练记录不变。大规模两档NSGA比较各3次均为N→P=0、P→N=1，必须与上述不等预算和单案例局限一并解释。

完整数值审核覆盖63档案、1023份动作日志及6390份按实例去重保存/最终方案，原完整MILP矩阵、界和整数性核验通过，违规0。独立智能体另行核验表4、全部跨规模和八个非基准敏感性情景、27覆盖配对、9情景联合前沿及正文表图。正式稿为 `供应链管理写作/数值实验与结果分析_论文稿.md`，公式仍为文字LaTeX，三种敏感性图均保留。最终含文稿证据以 `audit/numerics-with-report.json` 与独立终验的当前文件哈希为准，不将历史或暂存证据冒充最终PASS。

含文稿数值终验PASS，SHA256 `3fc7bdeef3a49a91d551081e86dcf849c6000a1d2f8269339382920eb3b513db`；对应正式Markdown SHA256 `198666d3bdb2b55b1ce4e69babca519533f8cbf8f391509ddf68bd01776a9324`。Git暂存区干净导出亦通过171项测试（13.547 s）和冻结协议哈希校验。未来重建表图可能改变图像元数据哈希，须重新绑定终验，不能直接沿用本次证书。
