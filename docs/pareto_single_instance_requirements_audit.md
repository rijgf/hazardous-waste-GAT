# 单实例帕累托实验：独立需求与交付验收

审计对象：`供应链管理写作/实验要求哦.md`（302行版本）及本轮新增实现、原始实验材料、`供应链管理写作/数值实验与结果分析_论文稿.md`。审计日期：2026-09-08。

## 当前结论

**FINAL SPEC PASS：本轮全部硬性交付要求已完成核验，表4、表5、表6、附表及图2完整；无未关闭的Spec阻断问题。验收仅针对下述最终论文哈希及锁定实验版本。**

本文件由独立需求审计智能体维护；代码和论文稿由实现智能体写入，数值复算另由独立数值审计完成。正式验收逐项依据真实文件和复算证据，不根据实现者自述直接勾选。

## 约束优先级及消歧

1. 用户最新明确约定及根目录 `AGENTS.md` 优先：实验每个所需测试规模1个实例，不改变训练；使用登记的 New-S / New-L 冻结推理，不训练、不替换、不改模型参数。
2. 需求第184行“各由一个独立训练实例训练”是过时描述；当前模型实际每种训练规模24实例。正文应准确披露24，不按该旧句重新训练。
3. 需求第125—129行模板MILP目标列“—”与第131行“填单次求解值”、第133行差异公式、第294行验收存在表面不一致。本轮填写真实MILP目标值，确保可核验。
4. 需求已指定表4保留原小规模实例（第7、102行），因此不应用默认 `Test-1/000.json` 替换。该默认只用于未另行指定的四规模前沿测试。
5. 单实例结论只能表述为固定案例观察；三重复样本SD表示求解随机性，不是跨实例统计或训练规模因果效应。
6. 输出仅修改Markdown论文稿，不修改任何Word文档；现有锁定数据、v2历史3000次结果和两份pt保留。

## 已核对的身份信息

### 模型

权威登记：`configs/frozen_ppo_models.json`。以下为读出的预期值，正式运行前仍须对实际文件计算哈希。

| 模型 | 检查点 | SHA256 |
| --- | --- | --- |
| Train-S / New-S | `outputs/reference_generalization_v2/models/small_model.pt` | `25b48dadc776f21a7e0aacfeea12f2cfe92632f1ba4fb3e0c856ac25b26ea565` |
| Train-L / New-L | `outputs/reference_generalization_v2/models/large_model.pt` | `29165c0a0b9d1c9b5e843f948049afb1bb94692e0dbe6aa161982b425f73bfef` |

两份模型的 `objective_normalization=instance_reference`；必须使用 `PPOImprover.from_frozen_checkpoint(..., objective_refs=(b_C,b_R))`。

### 原小规模固定偏好案例

现论文稿内部核验说明与 `docs/stage_report_deep_rl_experiments.md` 第304—306行相符：

| 字段 | 期望身份 |
| --- | --- |
| 五维规模 | 3/2/2/3/2 |
| 实例种子 | 3723524230 |
| 实例规范JSON SHA256 | `c61c8fc4c2d43afb69fb76d26a69ef7a246a77ff11b451396b302849988a803c` |
| 初始/参考方案种子 | 3949079119 |
| 参考方案规范SHA256 | `5fdfb1130823c21856db79670ab76cbbbac336b68ac2548cdf8c3b81d1a791f3` |
| 已记录固定b | b_C=1384.991951；b_R=10.876517565143931 |

原稿说明 `outputs/20260825_131529_507270` 当前缺失。只有汇总文字而无方案、实例、有效时限证据，不足以复用旧MILP。若从已记录配置和种子重建原实例/参考方案，须逐项匹配上述身份并披露重建，不重新选择实例或b。旧常数编码PPO不能复用，须重新运行新冻结模型15个偏好—重复单元。

## 正式运行前门槛

| ID | 要求与需求行 | 需留存的证据 | 当前状态 |
| --- | --- | --- | --- |
| G01 | 模型登记、哈希、固定b（25—29） | 实际pt哈希、加载元数据、缺b/非正b拒绝测试、运行前后模型一致 | PASS（预检及正式记录核验） |
| G02 | 单实例锁定（7、102、143、173） | 原小例身份；Test-1…4各000；L20与Test-4身份一致；无训练重叠 | PASS（预检及正式记录核验） |
| G03 | 七类token及动作对象完整（280） | 四规模token分项/上界、容量896、对象上界512、不静默裁剪证据 | PASS（预检及正式记录核验） |
| G04 | 覆盖数学规则（61—83、300） | 同集合双向1、严格优劣方向、重复不变、空集NA、各自先ND、失败状态测试 | PASS（预检及正式记录核验） |
| G05 | 容差固定（81） | 未舍入C/R、实例固定b尺度、1e-8；去重与弱覆盖规则一致 | PASS（预检及正式记录核验） |
| G06 | 候选档案与回放（39—43、284—288、300） | 搜索中非加权改进但非支配候选可入档案；完整方案读写复算；档案各自ND | PASS（预检及正式记录核验） |
| G07 | 真正NSGA-II（51—57） | 排序、拥挤度、等级+拥挤度二元锦标赛、父子代精英环境选择 | PASS（预检及正式记录核验） |
| G08 | 多周期决策表达（55） | 周期、车辆、设施、顺序可变；同实体跨期可重复服务；同一期至多一次、访问全收 | PASS（预检及正式记录核验） |
| G09 | 预算预锁（53、149—157） | population100、cx0.9、mutation0.25；PPO40320、B1=40320、B2=120960；不按测试胜负调参 | PASS（预检及正式记录核验） |
| G10 | 预算真实计数（153—155） | 重复/失败计入候选；初群新增计入；评价/缓存/失败分别记录；B2续B1、末代未满计数 | PASS（预检及正式记录核验） |
| G11 | 敏感性可行性与单因素（226、232、238） | 9版本逐字段diff；各自预绑定b；车辆/设施利用率和松弛量；基准不重算 | PASS（预检及正式记录核验） |

## 正式实验单元账本

| 实验部分 | 必需原始单元 | 复用条件 |
| --- | --- | --- |
| 表4小例 | 5偏好×3次新Train-S=15次PPO；5次MILP | MILP历史复用须实例、参考方案、b、目标、3600有效时限、完整可回放方案全部匹配 |
| 表6跨规模 | 2模型×4固定实例×3完整前沿=24次PPO | 不复用旧五偏好标量值或旧模型 |
| 表6 NSGA基线 | 4固定实例×3完整前沿=12次NSGA B1 | 同规模两PPO共用相同3条NSGA记录 |
| 表5 L20 | 3次Train-L/Test-4 PPO；3次NSGA B1和续跑B2 | PPO/B1直接复用表6相同记录；B2在同一NSGA演化继续，不是额外独立重启 |
| 图X敏感性 | 9情景×3完整前沿=27条PPO记录 | 基准3条与表5身份/模型/起点/b/种子/预算全同则复用，新增24条 |

新增正式PPO完整前沿总计48条（泛化24+扰动24），每条21权重×60步×32尝试。表4另计15次单偏好，不混入完整前沿次数。主表有24组表6配对及表5额外3组B2配对，表5 B1同表6 Train-L/Test-4，不把复用重复计为独立实验。

## 交付验收矩阵

| ID | 交付要求与需求行 | 必须满足的检查 | 当前状态 |
| --- | --- | --- | --- |
| D01 | 表4结构（116—135、294） | 5偏好×2方法=10行，方法在左；仅一个J列；C3位/R4位/J6位/差异3位/秒2位 | PASS（见最终证据） |
| D02 | 表4选解（131—133） | 3次中J最小的同一实际方案C/R；预定repeat破平局；非三次均值、非分别取最小C/R | PASS（见最终证据） |
| D03 | 表4差异/状态（106、112、133） | 100(PPOmin−MILP)/MILP；限时†不是最优gap；零分母报绝对差异；无伪最优断言 | PASS（见最终证据） |
| D04 | 表4时间（106、133） | PPO三次搜索总耗时、MILP单次墙钟；训练独立；缺失写未记录，不代填 | PASS（见最终证据） |
| D05 | 表5（141—167、295） | 仅L20单行实例数1；B1/B2各双向；3配对均值±样本SD；B1=表6对应格 | PASS（见最终证据） |
| D06 | 表6（173—205、296） | 2模型行×4组×2方向=8数值列；PPO对NSGA而非S对L；同规模共用NSGA；不跨实例/规模平均 | PASS（见最终证据） |
| D07 | 配对有效性（83、94、286） | 计划/有效/失败数；空前沿NA；有效<2时SD NA；保留每次双方点数、覆盖数、覆盖率、容差 | PASS（见最终证据） |
| D08 | 前沿附属记录（43、205、286） | 每次完整时间、点数、成功率、可行性、期末清零；候选/评价/缓存/失败/停止原因可追溯 | PASS（见最终证据） |
| D09 | 敏感性搜索（228—240） | 9情景每3运行；同种子/预算；每情景真重优化、自身固定b；不以跨情景J排序 | PASS（见最终证据） |
| D10 | 图X内容（244—262、297） | 两并排子图、各五系列；颜色标记一致；真实点、各情景独立ND；无平均坐标或跨情景筛点 | PASS（见最终证据） |
| D11 | 图X表达和文件（250—258） | 实际或模型单位、图注含实例/模型/21权重/3运行联合/扰动/状态；脚本+CSV+SVG或PDF+300dpi PNG | PASS（见最终证据） |
| D12 | 可复现材料（284—288、298） | 实例、配置、代码版本、种子、模型哈希、参考与搜索起点、全部方案、前沿、覆盖配对互联可重算 | PASS（见最终证据） |
| D13 | 论文叙述边界（15、47、94、157、184、205、234—240） | 不声称邻域网络迁移/完整真实前沿/统计泛化；不把同尝试当同时间；容量曲线重合如实解释 | PASS（见最终证据） |
| D14 | 旧内容不混入新交付（3、106、203、288） | 去除旧50实例v1、旧GA比较/宏平均J、旧模型训练时间、旧‘未做敏感性’等矛盾文字 | PASS（见最终证据） |

## 审计过程记录

- 已全文阅读根 `AGENTS.md`、冻结模型登记、302行需求和原论文稿。
- 已核对原小例的种子/方案哈希与阶段报告相符；工作区文本搜索未找到原方案文件的可回放证明。
- 已向实现智能体及根智能体发送单实例、固定b、真候选档案、NSGA多周期、预算计数、测试门槛、旧稿冲突及表4复用条件。
- 初次需求解读时尚未拿到预检产物，当时未批准为“正式实验已通过预检”；后续放行证据见下。
- 独立执行当前 `tests.test_pareto_experiment` 的4项基础测试，均通过；实际两pt SHA256均与登记一致。此结果只覆盖该测试范围，不等于最终交付通过。

### 开放问题 R01：容差相等代表替换残留被支配点

**状态：已关闭（正式运行前）。** 需求第81、300行要求算法档案内部无严格被支配点。

`src/pareto_experiment.py` 的 `ParetoArchive.add` 在近似相等点用较小方案ID替换既有代表后立即返回，没有清除被新代表严格支配的其他点。独立复现：令refs=(1,1)、tol=1e-8，依次插入A=(10,10,id=`b`)、C=(10+2e-8,10-1.5e-8,id=`c`)、B=(10+0.9e-8,10-0.9e-8,id=`a`)。最终档案仍保留B和C，但当前实现的 `strict(B,C)` 为True。独立数值审计也以另一组目标点复现同一问题。

修复已核验：相等代表替换时同时去除与新点相等或被新点严格支配的旧点；`sorted_points` 明确断言无严格支配与重复；新增 `test_equal_representative_replacement_removes_newly_dominated_points`。独立再次执行包含此回归的6项Pareto测试，全部通过。最终正式档案仍需全量数值核验。

### 正式运行前需求门槛结论

对应协议SHA256：`69b24fd138602e7b2de82063c92a7db5e9daa390fe2192373c4d95358acdb49a`；预检产物：`output/pareto-single-instance-v3/preflight.json`。前序草稿保留在该目录的 `protocol-drafts/`。

- 6项独立重跑的单测通过，覆盖弱覆盖方向/相等/重复/空集、容差替换回归、NSGA排序拥挤度与候选预算、四类基因对严格可行表型的实际改变、观察器不改变原PPO方案哈希与逐步轨迹。
- 13份实例或参数版本的参考方案严格可行且b匹配；Test-1至4容量上界分别为48/113/242/758 token、12/36/90/320对象，均在896/512内。
- 四规模各000，表4原小例另行保存，8扰动仅单因素副本；固定b、任务清单、配对seed与48次新增完整PPO/12次NSGA结构符合需求。
- 预检已记录车辆/处理处置利用率及最小容量松弛量，补齐最初只记录处理利用率的缺漏。
- 大例2步64次PPO候选及NSGA120/200尝试预检通过；保存方案重新评价C/R一致。
- 正式共享方案输出的临时文件由固定 `.tmp` 改为含进程ID与纳秒时间的唯一临时文件，已消除先前发现的跨进程共同方案写盘竞争风险。
- 已向根和实现者给出“需求侧可开正式作业”的意见；需结合独立数值审计放行。运行期间不得修改计算源锁、注册表或pt。

以上证据使G01—G11的正式前置检查在已锁协议上通过；矩阵中的交付状态仍须待正式结果逐项更新，不提前勾选D项。

## 最终Spec结论

最终结论为PASS，证据映射与交付哈希见文末。判定综合原始文件、独立数值复算、论文逐段核对及图2视觉检查，并非依据实现者自述或退出码0。

## 正式计算期间的报告模板审计

已完整阅读 `report_pareto_experiments.py` 和 `plot_pareto_sensitivity.py` 初稿。此时正式计算尚未结束，以下为模板级检查，不是最终数值或渲染验收。

已确认模板基本逻辑：表4使用五偏好各3次中最小J实际方案及三次累计时间；表5/6覆盖方向与重复统计正确，B1复用对应Train-L/Test-4；表6两模型对共同NSGA基线而非彼此比较；训练每规模24实例、实验每规模1实例；9敏感性情景3次联合真实非支配点，图脚本不平均坐标、不跨情景筛选；350dpi PNG和SVG输出；总体保持固定案例而非跨实例推断。

已发实现者的模板修订项如下，正式交付前需核验关闭：

| ID | 要求/证据 | 需修订内容 | 状态 |
| --- | --- | --- | --- |
| R02 | 需求83、94、205；模板表6注释“未输出空前沿的配对数为0” | 语义倒置，应为“空前沿失败配对数为0”；计划/有效/失败统计按真实配对生成，非硬编码；None覆盖值不可进入未经筛选的mean | 已关闭 |
| R03 | 需求246最终图号；已只读提取现有正文整合稿DOCX图题，唯一已有图1为训练框架 | 新敏感性图正式编号图2，全文/图注/文件说明交叉引用一致，不留图X占位 | 已关闭 |
| R04 | 用户要求所有表格的方法置于左侧 | 附表A3当前仅情景列，应在左侧注明PPO（Train-L），其他表方法左置保持 | 已关闭 |
| R05 | 需求236要求结合周期、车辆、共载组成、风险分项 | 已有端点风险分项和车辆—周期路线次数，但缺实际共载组成；补确定规则选择的真实路线例，包含周期、车辆、设施、访问顺序、品类及稳定方案ID | 已关闭 |
| R06 | 需求43、133、157真实计时口径 | 明示PPO前沿时间含逐偏好进度I/O；NSGA B2从运行开始累计且包含B1档案落盘；最终本预算导出边界不混淆 | 已关闭 |
| R07 | 需求238不人为拉开容量曲线；根独立推导本实例容量全域冗余 | 处理能力原为2Q_s，最低扰动后1.4Q_s仍高于任一设施单期可能处理量上界Q_s，容量扰动不改变真实可行域/原始目标；不能把前沿微移解释成放松约束收益 | 已关闭 |

图号核查仅只读提取已有Word正文图题，没有修改或导出任何Word文件。根已确认v3正式计算自始使用16个worker，8个worker仅为先前建议，不属于本轮正式批次；因此模板16进程记录属实。

### 最终表4门槛 R08：MILP整数容差引起的评价不一致

**状态：已关闭；统一派生规范化及独立数值核验通过。** 正式p3/p4原始文件到达时，两个记录均为 `strict_feasible=true` 但 `objective_consistent=false`。p3的原始解码评价J=0.6776815055而solverfun=0.6627756403；p4分别为0.3387821829和0.3320332497。报告程序的断言正确阻止了当时出稿。

根和独立数值审计定位到接近0的整数弧变量与微小连续流量触发离散启用/共载评价。要求保留原始日志、向量与metrics；只能另存固定数值清理规则、逐变量前后差异、原始文件哈希、清理后完整矩阵/整数/上下界残差、严格可行性及评价目标一致性证据。若采用派生规范化解，论文表4须明确该处理并使用其同一方案的C/R/J，不把solverfun和另一方案的伴随成本风险混拼。

关闭证据：`normalize_pareto_milp.py` 只构建/复核原矩阵，不调用求解器；五组统一整数舍入及连续绝对值小于1e-5置零，原文件不覆盖，派生记录位于 `milp/validated/`。完整矩阵最大残差6.03e-6小于原严格阈值1e-5，物理评价J与原求解器目标最大差3.85e-8小于1e-7；独立数值审计对五组全部重建验证。原最优状态不升级，p3/p4仍限时可行。最终论文第46、48行明确处理规则、原始/派生链接、无新搜索、后处理时间独立及表中同一派生方案C/R/J。

## 最终交付证据与硬门槛

最终论文：`供应链管理写作/数值实验与结果分析_论文稿.md`，SHA256 **`d983567c6fe46fabab235745b6840997660e61eba69eae26f7eab25e31478b1a`**。

正式协议SHA256：`69b24fd138602e7b2de82063c92a7db5e9daa390fe2192373c4d95358acdb49a`。报告脚本SHA256：`e8ef182df929f1c45aabaa44279339e8353d72d0867a80fc0ba2d7d5a6e14737`。计算源文件和两份冻结pt均独立重新核对，未改变。

| 硬交付 | 最终证据 | 结论 |
| --- | --- | --- |
| 表4（需求102—135、294） | 论文31—50行，10方法行仅新PPO/MILP，15次新PPO原记录、5个原MILP及5派生完整向量；最小J同方案选取、三次累计时间、3组最优/2组限时†；纯风险差异4.041%诚实保留 | PASS |
| 表5（需求141—167、295） | 论文56—75行，两层预算表头，L20明确1实例；27条 `coverage_pairs.csv` 中Test-4/large六条对应B1/B2各3条；B1=表6对应格，B2沿原演化继续 | PASS |
| 表6（需求173—205、296） | 论文81—105行，2行×8覆盖数值列，4个000固定案例、24完整配对、共用12次NSGA B1；每格3重复样本SD，覆盖指标为𝒞而非成本C | PASS |
| 图2（需求211—262、297） | 论文111行起图题与图注，实际PNG已打开目检；两并排子图、五情景跨图颜色/标记一致、真实散点、共用坐标、单位与中文完整；PNG4335×1736，读取DPI为350.012；SVG及脚本存在 | PASS |
| 敏感性材料 | 9情景各3运行，基准复用；`sensitivity_points.csv` 与 `sensitivity_scenarios.csv`；基准/−30%/+30%风险端点的周期、车辆、设施、访问顺序、实际分品类载量和稳定方案ID在正文展示；无均值伪前沿 | PASS |
| 时间与附表 | A.1两训练记录，A.2的8模型—规模行及5NSGA预算行，A.3九情景均方法置左；完整前沿均值/三次累计/MILP单次及后处理/NSGA B2累计边界分别说明 | PASS |
| 完整原始材料（需求284—288、298） | 60正式任务、63预算档案、4370前沿点出现、1008个PPO末次权重方案、15小例PPO、5MILP及规范化证据；代码、模型哈希、实例b/参考方案、种子、方案及CSV互联 | PASS |
| 科学表述边界 | 论文明确24训练实例/模型、实验单例、无微调、无换例、无跨实例统计推断；覆盖率非最优性距离；B2反向覆盖0不等于全支配；处理能力全域冗余证明完整，不夸大容量放松收益 | PASS |

### 独立对照与数值陈述

- 需求审计者全文阅读最终稿，直接核对表4逐偏好CSV、表5 B1/B2的6条未舍入覆盖配对、敏感性9情景端点/风险分项/路线组成CSV；关键正文数字与对应记录吻合。
- B2三条NSGA对PPO覆盖分别0.6938775510、0.6885245902、0.6779661017，PPO反向均0；均值±样本SD为0.6868±0.0081与0±0。正文没有将其夸大为NSGA覆盖全部PPO方案。
- 独立数值审计 `audit/independent_numerics.json` 为PASS，错误/缺失均为空：4309个唯一前沿方案（4370出现）、1008个PPO权重末次方案、27覆盖配对、15小例方案、5个MILP完整向量及统一规范化均重新验证；6个CSV、主表13数据行及附表24数据行核对通过，SVG核对690个真实点标记（两子图基准各显示一次）。
- 最终8个Markdown相对链接逐个在论文目录下解析，全部存在；不依赖旧失效D盘路径。
- 只读检查 `git diff --name-only 820ab1c -- '*.docx' '*.doc'` 及对应 `git status` 均无Word变化。本轮未修改或导出任何Word文档；需求审计者仅为图号读取已有Word图题。

### 需求冲突最终处理

需求第184行“各由一个独立训练实例”与用户已冻结模型及最新 `AGENTS.md` 冲突，按用户最新确认的实际24训练实例/模型执行；正文第11行及A.1准确披露24，没有因此重新训练。实验仍每个需要规模仅1案例，训练与实验实例数量不混淆。需求表4模板的MILP J占位横线按第131/133/294行实义填写真实J，以保证差异可复算。MILP未暖启动，正文区分其与PPO/NSGA-II共享参考搜索起点的范围。

**最终Spec轴：缺失0、错误实现0、未关闭阻断0；全部硬交付PASS。** 本结论不代替根智能体的Git提交/推送结果确认，也不意味着单案例结果构成普遍统计结论。
