# 图表视觉与来源检查

2026-09-10，检查实际生成的 `figures/ppo_nsga_best.png` 和 `figures/ppo_nsga_all_repeats.png`。

- 主图的中文标题、成本/风险坐标轴、三组图例完整可读，无裁切或遮挡数据；圆、方、三角标记配合颜色区分方法。
- 三次配对图共用坐标范围，图例与子图标题分离；三次结果均展示，没有只保留有利运行。
- 每条线使用同一次运行的全部实际非支配点，按成本排序连接。大跨度线段保留，不将线段插值宣称为可行方案；无平均点或平滑拟合点。
- 主图三个方法按同一归一化参照点 (1.1,1.1) 的最大 HV 选择，实际均为第2次。表5/6仍采用全部3次配对。
- 正式Markdown公式内容与备份一致，不使用公式图片；表格列数和图片相对路径由含文稿审核检查。本记录没有声称截取并检验 Codex 自身的 Markdown 渲染界面。

终验发现的重复方案目标浮点差异最大绝对值为 4.55e-13，采用严格浮点比较容差而非要求逐比特相等；没有更改原始结果。暂存稿统一为 LF，与 apply_patch 发布后的正式稿逐字节核对，避免 Windows 文本写出换行差异影响哈希。
