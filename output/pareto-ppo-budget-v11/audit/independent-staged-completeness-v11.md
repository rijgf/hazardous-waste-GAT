# V11 暂存稿独立完整性核验

结论：STAGED PASS，无阻断正式发布问题。本记录不等同于正式交付终验；正式发布后的文稿哈希和含文稿 numerical audit 尚待绑定。

使用 scholar-verify 的 completeness 路线，只读实验实现、输入、方案和文稿，另存本核验记录。未训练、调用网络、增加搜索、重算 NSGA/MILP 或修改正文。

## 当前证据

- 暂存文稿 SHA256：8d94beb7a58c447a6c71db821712d75a3a9b83793ddc0d4f9078e5d930d505f7。
- 五份独立阶段证据覆盖 13 个实例/情景，6390 份按实例去重保存及最终方案完成原 MILP 全矩阵回放，0 违规，最大行残差 1.7763568394002505e-14；历史五个 MILP 返回向量另外核验，不能混用这两个残差口径。
- 当前重新校验了 7030 个阶段引用的源码、前沿、日志、方案、执行记录和历史 MILP 文件哈希；加上图数据来源文件为 7031 个。均匹配阶段证据。逐阶段哈希与文件映射摘要保存于同名 JSON。
- 48 份新 PPO 完整前沿、15 份复用 NSGA 档案、15 次小规模 PPO、5 组复用 MILP 齐备；1023 个完整日志累计记录 9606232 次 PPO 候选。没有将真实提前耗尽补成上限。

## 原始结果到文稿

- 全部 8 个 CSV 均读入核验。63 档案的 5844 个前沿点与原 C/R、方案身份及来源一致；27 对覆盖的分子、分母、方向与独立广播计算一致。
- 1008 次前沿偏好和 15 次小规模偏好预算 CSV 与独立逐日志记录一致。小规模每偏好选三次中真实最优的一份计划，C/R/J 来自同方案，时间为三次累计。
- 九情景重新汇总三次前沿，以独立 NumPy 支配判定联合筛选，共 1445 点。全部端点、原始 C/R、四风险分项、车辆—周期路线数、服务次数、示例路线返回载量和联合最大处理利用率均与 CSV 一致；联合点没有跨情景筛除。
- 表4、表5、表6和附表逐数值比较：均值±样本标准差、显示精度和时间口径一致。历史训练时间来自 V9 completed 元数据，本轮没有训练。
- 三种图保留同一批真实数据；平滑版本逐节点核对 PCHIP，所有原始点保留，无外推、无新增优化解。已独立视觉查看三 PNG，坐标、图例、标签可读，无裁切。
- 七张 Markdown 表列宽一致，三个公式为文本数学块，没有公式图片，20 个本地链接有效（与同名 JSON 实际解析计数一致；初次手写概述误记为21，现纠正）。没有以图像查看冒充 Codex 预览界面的直接截图核验。

## 结果解释

表5的两档比较均为 N→P=0、P→N=1；正文没有倒置覆盖方向，也未把不同预算、不同并发争用条件称为公平对比。四规模的单实例及 Test-4 反馈调参局限、冻模推理、按实例绑定 b、NSGA/MILP 历史复用均明确披露。

处理能力与风险端点的百分比变化已独立复算并保存在 JSON。敏感性展示为固定算法与固定实例的响应，不是参数因果识别。历史 p3 MILP 源 proven_optimal=false 保留，正文说明求解器零 gap 证明经数值容差规范化核验后的报告层交接，而非改写源标志或新求解。

## 后续门槛

正式发布后重新锁定正式文稿、report_verification 和 numerics-with-report 的实际哈希，确认后者 report 非空且绑定当前正式稿与 verification 哈希（V11 使用该结构，不是旧版的 report_checked 字段）；任何旧 PASS 或本暂存 PASS 均不能替代该门槛。Git 提交推送由主代理处理，不在本只读数值核验范围内。
