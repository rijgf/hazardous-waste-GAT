# 提交包预检（不是最终交付审核）

使用Git暂存内容导出到新建临时目录，不使用当前工作区的未暂存文件。检查目标是模型/代码/数据依赖完整性，不启动正式训练或搜索。

- 75个冻结计算/模型/训练来源依赖在索引中的字节与工作区完全一致。
- 新预算适配9项测试在干净副本通过。
- 首次完整171项测试有2项报缺少 `configs/ppo_objects_v8_exploratory.json`：V9历史训练来源检查依赖该登记。该文件随后已补入提交包；这不改变实际使用的V9 PT。
- 补齐后同一干净副本完整171项测试通过，耗时20.200秒。
- `.gitattributes`补充JSONL/gzip/PowerShell原字节保护，避免签名日志和旧调度脚本因换行转换破坏SHA256。

本检查发生于全部前沿尚未完成时，不声称所有正式结果已纳入索引、也不替代最终论文哈希/数值审核。最终提交前仍需刷新完成产物并检查索引内容。
