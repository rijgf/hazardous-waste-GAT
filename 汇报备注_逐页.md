# 课题二进展汇报7.9 逐页汇报备注

## 第1页 封面

各位老师好，我今天汇报的是课题二目前的阶段性进展。研究主题围绕危险废物多周期收运与处置优化，重点关注多末端处置能力受限、不相容危废不能混装，以及库存随时间演化带来的风险耦合问题。

这次汇报主要讲四部分：第一是问题背景和当前完成情况，第二是优化模型，第三是 PPO+Transformer 改进算法，最后是目前的小规模和大规模数值实验结果，以及下一步工作安排。

## 第2页 进展总览

这一页先给出整体研究对象。我们考虑的是危废从产生、暂存、运输、进入处置中心接收，再到分期处理的全过程。这里不是单纯做车辆路径规划，而是要同时决定什么时候收、哪辆车收、路线怎么走、送到哪个处置中心，以及处置中心每期处理多少。

这个问题的核心矛盾是成本和风险之间的权衡。如果为了降低车辆启用和运输成本，倾向于集中运输，产废端库存会积压，暴露风险会上升；如果为了降低库存风险而及时收运，又会增加车辆使用和运输频次。因此模型要在低成本集中运输和低风险及时收运之间找到平衡。

目前已经完成了三类阶段性工作：第一，建立了多周期成本—风险联合优化的 MILP 模型；第二，搭建了 PPO+Transformer 的改进式强化学习算法；第三，完成了大小规模数值实验和消融实验框架。后续重点是继续优化算法结构和求解时间，特别是提升高风险偏好下的表现，并开展参数敏感性分析和论文写作。

## 第3页 优化模型

这一页说明数学模型的结构。左侧是系统结构：首先把“产废单位—废物类型”组合成等效产废节点，每个节点有分期产生量和产废端库存；中间是车辆在不同周期执行收运路径，并产生同车共载关系；右侧是处置中心，它有技术匹配关系、接收库存和分期处理能力。

目标函数采用成本—风险加权优化。成本包括车辆启用成本、运输距离成本和危废处理成本；风险包括基础运输风险、不同危废共载带来的耦合风险，以及产废端和处置端库存风险。通过调整 αc 和 αr，可以表达成本优先、风险优先或折中偏好的不同决策场景。

约束部分主要保证方案在业务上可执行。路径与车辆约束保证车辆从同一处置中心出发并返回；服务与收运约束保证访问则全量收运；相容性约束处理技术匹配和不相容危废禁止同车；容量约束限制车辆载重和两端库存；库存流转与处理能力约束则刻画跨期递推和有限处理能力。

## 第4页 训练阶段算法设计

这一页讲 PPO 智能体是怎么训练出来的。核心思想是“改进式 DRL”：不让神经网络直接从零生成完整路径，而是先用贪心方法构造一个可行解，然后让智能体学习下一步应该使用什么局部搜索操作来改进这个解。

流程从左到右看：先构造训练问题实例，再由贪心方法生成初始解；当前解会被编码成状态，输入 PPO 智能体。智能体内部包含 State Encoder、Transformer 和 Policy/Value Heads，用来理解当前解结构，并输出策略和价值评估。

随后智能体选择局部搜索算子和作用对象，生成候选解，再通过 repair 与评价模块判断候选解质量。如果候选解带来改进，就得到正向奖励；如果动作无效或无法修复，就给予惩罚。这样反复交互后，PPO 参数会逐步更新，最终学到一套高质量的局部搜索改进策略。

## 第5页 求解阶段与动作空间

训练完成后，求解阶段不再更新网络参数，而是把训练好的 PPO 策略作为启发式改进器使用。面对一个新实例，先生成贪心初始解，再把当前解编码输入策略网络，由策略网络指导局部搜索。

这里的动作不是一个简单标签，而是四元组 a = (operator, object1, object2, object3)。operator 决定采用哪类局部搜索算子，后面三个 object 参数决定作用在哪些任务、路径、车辆或周期对象上。这样设计的好处是动作空间比较通用，可以覆盖不同规模实例。

当前使用了 5 类局部搜索算子：Relocate 做任务迁移，Swap 做任务交换，2-opt 做路径内顺序调整，Change facility 更换处置中心，Delay / Advance 做跨周期调整。每一步执行后只接受质量更好的候选解；如果没有达到停止条件，就继续迭代改进，直到输出最终优化方案。

## 第6页 State Encoder 与策略网络结构

这一页是算法的技术细节，可以分成三块来看。左侧是状态表达：我们把完整解拆成 7 类结构化 token，包括全局统计、偏好权重、周期信息、车辆信息、产废节点信息、处置设施信息和已使用路径弧信息。

中间是策略网络结构。每个 token 是 18 维特征，最多保留 768 个 token；不同类型 token 先经过分类线性投影，变成 48 维 embedding，再输入标准 Transformer Encoder。之后通过 Masked Mean Pooling 得到全局状态表示 z，最后由 5 个输出头分别输出算子、三个对象参数和价值函数 V(s)。

右侧是训练与工程实现。当前训练采用 30 个并行 episode，每个 episode 24 步，因此每轮 PPO 更新会积累 720 条状态转移。更新时使用 GAE 优势估计、clip 约束、熵正则和价值函数损失。推理时，每一步会采样多个候选动作，执行局部搜索，并保留带来改进的方案。

## 第7页 数值实验结果

这一页展示当前阶段的数值实验结果。评价指标采用加权归一化求和，而不是直接比较原始成本和风险，因为两者量纲差异很大。具体做法是以启发式初始解为参考，把当前方案的成本和风险分别除以参考方案的成本和风险，再按偏好权重加权。

从小规模结果看，PPO 在不同偏好下都能进一步改善启发式初始解。在风险偏好较低时，PPO 与 MILP 最优解的平均差距约为 3.97%，说明当前方法已经有较好的解质量；但在风险偏好较高时，PPO 与最优解的差距会变大，说明风险优化能力和偏好识别能力还需要继续增强。

从大规模结果看，由于没有 MILP 精确最优解，我们主要比较 Heuristic、GA-light 和 PPO。PPO 在所有偏好下都能明显降低归一化加权目标，尤其在风险权重提高时，风险值下降更明显。主实验汇总中，大规模 PPO 平均归一化目标约为 0.617，优于 Heuristic 的 1.000 和 GA 的约 0.958。

补充说明一下消融实验：之前大规模消融曾出现所有模型结果都等于 1 的异常，后来定位到 repair 逻辑会导致 large 场景动作全部修复失败。修复后重新加载已有模型评估，所有 PPO 消融解均可行，平均归一化目标约在 0.624 到 0.659 之间，其中 hybrid Transformer-FC 当前略优，去掉 route-arc token 后表现下降更明显。

## 第8页 结束页

以上是目前课题二的阶段性进展。总体来看，模型层面已经能够覆盖多周期、库存演化、不相容共载和处置能力约束；算法层面已经形成了“贪心初始解 + PPO 指导局部搜索 + repair 评价”的改进框架；实验层面也已经完成了小规模、大规模和消融实验的初步验证。

下一步我会继续从三个方向推进：第一，进一步提高 PPO 对风险偏好的识别能力；第二，优化动作选择和 repair 逻辑，减少无效动作并缩短求解时间；第三，补充参数敏感性分析，重点考察车辆容量、事故概率、风险权重和处置能力变化对结果的影响。

我的汇报到这里，感谢各位老师的建议与指导。
